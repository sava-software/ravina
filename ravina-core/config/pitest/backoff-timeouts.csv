# Audited timeout-detected mutants (see AGENTS.md, "a new timed-out mutant is
# a reviewer-stop"); structural causes in README.md, "Audited timeout sets".
# Deliberately empty since 2026-08-05: this suite has no timed-out mutants.
# The file is kept so the audit stays armed — a first timeout here warns as
# an unaudited newcomer rather than drawing the softer adoption hint.
